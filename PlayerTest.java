//import static org.junit.Assert.*;

//import org.junit.Test;

public class PlayerTest 
{
	/*
	@Test
	public void testShipChecker()
	{
		Ship battleship = new Ship();
		battleship.setLife(0);
		ArrayList<Ship> shipsPlaced = new ArrayList<Ship>();
		shipsPlaced.add(battleship);
		shipChecker();

		assertEquals("Ship should be sunk.", shipChecker());

	}

	@Test
	public void testRotate()
	{

		Ship battleship = new Ship();
		battleship.reverseOrientation();
		Ship current = new Ship();
		setCurrent(battleship);
		rotate();

		assertEquals("Ship should be horizontal.",

	}
	*/
}